<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Super Hero Agency{% endblock %}</title>
    
    <!-- Feuille de style Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Vos styles personnalisés -->
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
</head>
<body>
    <header>
    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg" style="background: linear-gradient(90deg, #ff6b6b, #ff5c58); box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);">
        <div class="container">
            <a class="navbar-brand fw-bold text-uppercase" href="{{ path('app_super_hero_index') }}" style="color: #fff; font-family: 'Comic Sans MS', Arial, sans-serif;">
                <i class="fas fa-shield-alt me-2"></i> Super Hero Agency
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" href="{{ path('app_mission_index') }}" style="margin-right: 20px;">
                            <i class="fas fa-clipboard-list"></i> Missions
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" href="{{ path('app_power_index') }}" style="margin-right: 20px;">
                            <i class="fas fa-bolt"></i> Pouvoirs
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" href="{{ path('app_statistic_index') }}" style="margin-right: 20px;">
                            <i class="fas fa-chart-bar"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" href="{{ path('app_team_index') }}">
                            <i class="fas fa-users"></i> Équipes
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

    <main class="container py-4">
        {% block body %}{% endblock %}
    </main>
    <footer class="text-center py-4">
        Powered by Super Hero Agency - 2024
    </footer>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
