<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Super Hero Agency{% endblock %}</title>
    
    <!-- Feuille de style Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Feuille de style Choices.js -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/choices.js/public/assets/styles/choices.min.css">
    
    <!-- Vos styles personnalisés (chargés après pour prioriser vos styles) -->
    <link rel="stylesheet" href="{{ asset('css/base.css') }}">
</head>
<body>
    <header>
        <!-- Barre de navigation -->
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_super_hero_index') }}">Super Hero Agency</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_mission_index') }}">Missions</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_power_index') }}">Pouvoirs</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_statistic_index') }}">Dashboard et Statistiques</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_team_index') }}">Equipes</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <main class="container py-4">
        {% block body %}{% endblock %}
    </main>
    <footer class="text-center py-4">
        Powered by Super Hero Agency - 2024
    </footer>

    <!-- Script Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Script Choices.js -->
    <script src="https://cdn.jsdelivr.net/npm/choices.js/public/assets/scripts/choices.min.js"></script>

    <!-- Initialisation de Choices.js -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const elements = document.querySelectorAll('.js-choices');
            elements.forEach(el => {
                new Choices(el, {
                    removeItemButton: true, // Permet de supprimer des options sélectionnées
                    placeholderValue: el.dataset.placeholder || 'Sélectionnez une option',
                });
            });
        });
    </script>
</body>
</html>
