{% extends 'base.html.twig' %}

{% block title %}Power{% endblock %}

{% block body %}
    <h1>Power</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ power.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ power.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ power.description }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ power.level }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_power_index') }}">back to list</a>

    <a href="{{ path('app_power_edit', {'id': power.id}) }}">edit</a>

    {{ include('power/_delete_form.html.twig') }}
{% endblock %}
