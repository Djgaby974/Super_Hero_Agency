<form method="post" action="{{ path('app_power_delete', {'id': power.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ power.id) }}">
    <button class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition-all">
        Delete
    </button>
</form>
